/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: ["picsum.photos", "images.pexels.com", "images.unsplash.com", 'www.shutterstock.com'],
  },
};

export default nextConfig;
