/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: ["https://picsum.photos", "https://images.pexels.com", "https://images.unsplash.com", 'https://www.shutterstock.com'],
  },
};

export default nextConfig;
